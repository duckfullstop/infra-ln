apiVersion: v1
kind: PersistentVolume
metadata:
  name: paperless-consume
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
  nfs:
    server: "vault.leachnet"
    path: "/mnt/decavault/services/paperless/consume"
